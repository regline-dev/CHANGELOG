# CHANGELOG.md

이 프로젝트의 설계 문서(README.md, AGENTS.md, 각 서비스 TASK.md) 변경 이력.
파일 자체에 버전을 표기하지 않고, 변경 사항은 이 문서에 누적 기록한다.

---

## 2026-06-28 (v2)

**변경 파일**: README.md, AGENTS.md, services/edge-collector/TASK.md, services/log-generator/TASK.md

**변경 내용**: edge-collector 포트 구조를 단일(8001) → 멀티포트로 변경

- edge-collector가 단일 HTTP 포트(8001)로 통합 수신하던 구조를 폐기
- 프로토콜별 멀티포트 동시 리스닝으로 변경: syslog/CEF(514, SIEM), HTTP/gRPC(8001, EDR), SNMP(162, 방화벽), NetFlow(2055, 방화벽)
- 이유: 실제 운영 환경 부합(장비가 원래 쓰는 프로토콜 그대로 수용), 확장성(프로토콜 추가 시 기존 구조 안 건드림)
- edge-collector/TASK.md: `listeners/` 폴더 신설, 구현 순서를 Step 1(HTTP만)→Step 1-1(나머지 3개 순차 추가)로 세분화
- log-generator/TASK.md: 전송 로직을 `send_siem()`/`send_edr()`/`send_firewall()`로 분리, 프로토콜별 분산 전송 명시

---

## 2026-06-28 (v1)

**변경 파일**: services/log-generator/TASK.md

**변경 내용**: 모순된 "운영 배포" 표현 정정

- "운영 배포 대상 아님(테스트 전용)"이라는 표현이 "그럼 테스트할 때도 실행 안 하나?"로 오해를 유발
- "운영 환경엔 영구 배포하지 않지만, 개발/테스트 중에는 직접 실행한다"로 명확히 정정 (하는 일/안 하는 일, 확정사양 표, Dockerfile 설명 3곳)

---

## 2026-06-27 (v3)

**변경 파일**: services/log-generator/TASK.md

**변경 내용**: "기존 코드 이전" 전제를 "신규 작성"으로 정정

- 0번 섹션에서 "기존에 작성된 코드를 이전한다"고 했던 부분이 사실과 다름을 확인
- 실제로는 Claude 작업 컨테이너에만 존재하던 예시 코드였고, 사용자 PC/프로젝트 폴더에는 없었음
- Step 1, Step 3, Step 5, 완료기준 전체를 "신규 작성" 기준으로 정정
- 확정된 사양(시간 기준 실행, 카테고리 비율 등)은 재논의 없이 유지

---

## 2026-06-27 (v2)

**변경 파일**: services/log-generator/TASK.md (신규 작성)

**변경 내용**: log-generator 작업 지시서 최초 작성

- 템플릿 3종(SIEM/EDR/방화벽) 기반 더미 로그 생성 사양
- 5가지 시나리오: 타임스탬프 갱신, 순서 랜덤화, 필드값 변조, 5% 비정상 끼워넣기, 폭주 재현
- 실행 방식: 시간 기준(`--duration`), 건수 기준은 폐기

---

## 2026-06-27 (v1)

**변경 파일**: README.md, AGENTS.md (신규 정리), CLAUDE.md/CLAUDE_short.md (삭제)

**변경 내용**: AI 작업 규칙 파일을 AGENTS.md 하나로 통합

- 기존에 CLAUDE.md(긴 버전), CLAUDE_short.md(짧은 버전) 2개가 혼재
- Cursor가 실제로 참고할 파일을 AGENTS.md로 단일화, CLAUDE 파일 2개 삭제
- README.md에 GitHub 구조(멀티레포+Organization), 개발 프로세스(Claude=설계, Cursor=구현) 섹션 추가

---

## 2026-06-23 ~ 2026-06-26 (누적)

**변경 내용**: 1단계 설계 정정 (edge-collector 핵심 구조 확정)

- **Fast Path 개념 신설**: 가드레일(3단계, 데이터 형식 검증)과 별개로, 1단계에서 즉시 위협 대응하는 Fast Path 도입. 블랙리스트 5종(IP/도메인/해시/계정/User-Agent), 판단 3등급(명확한 위협/애매한 패턴/정상) 확정
- **Fast Path 분리 여부 논쟁 → 분리로 최종**: "10단계까지 가면 100배 커질 것" 우려로 통합 제안했으나, "5종 다 구현할 거면 처음부터 분리"로 재확정. `fast_path/` 하위 폴더 구조 확정
- **파서 역할 오류 정정**: "파서가 벤더 포맷→공통 dict 변환"이라고 잘못 설계했던 것을 발견. 변환은 3단계(OCSF) 몫이며 1단계는 식별(분류)만 한다로 정정. `parsers/` → `classifiers/`로 이름 변경
- **Kafka 토픽 구조 확정**: 벤더별 15개 토픽이 아니라 소스 종류 3개(`logs-firewall`/`logs-siem`/`logs-edr`)만 사용, 벤더는 메시지 내 `vendor` 필드로 구분

---

## 2026-06-20 ~ 2026-06-21 (누적)

**변경 내용**: 전체 폴더 구조 및 README/AGENTS 최초 확정

- 11개 서비스를 "배포 단위(Docker 이미지 1개=폴더 1개)" 기준으로 확정 (여러 차례 통합 제안 나왔으나 모두 거부, 11개 유지로 최종)
- 1~10단계 전체 파이프라인 설계, 가드레일 원칙(1~2단계 없음, 3단계부터 시작) 확정
- LangGraph/LangChain 적용 범위 확정 (7~8단계 핵심, 6단계 LangChain만, 9단계 별도 그래프)
- OCSF 데이터 포맷, 통신 방식 규약 확정
- log-generator 5가지 시나리오 최초 설계 (이후 06-27에 TASK.md로 구체화)

---

## 기록 규칙

- 새 항목은 **최상단에 추가** (최신이 위로)
- 같은 날 여러 번 수정 시 `(v1)`, `(v2)`처럼 차수 표기
- 형식: `## 날짜 (vN)` → `**변경 파일**` → `**변경 내용**` (요약 1줄 + 상세 불릿)
- "왜 바꿨는지" 이유를 반드시 포함 (AGENTS.md "왜 바꾸는지 먼저 설명" 원칙과 동일)
